{{!-- profile.hbs --}}
<div class="container p-4">
	<div class="row">
		<div class="col-md mx-auto text-dark">
			<div class="card text-center">
				<div class="card-body">
					<h3 class="titulo">Bienvenido {{user.nomUsuario}}</h3>
					<h4>{{user.nickUsuario}}</h4>
					<a href="/incidencias" class="btn btn-primary m-4">
						Añade una incidencia
					</a>

				</div>
			</div>
		</div>
	</div>
</div>